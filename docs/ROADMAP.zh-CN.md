# 路线图

[English](ROADMAP.md)

## 阶段 0——项目基础

- 建立 NeoForge 项目、CI、许可证、独立实现政策和架构记录。
- 添加客户端和专用服务器的最小加载测试。

## 阶段 1——纵向功能切片

- [x] 与加载器无关的宾果卡生成、队伍标识、标准认领、锁定认领、计分和连线胜利规则。
- [x] 为确定性生成和首批规则添加单元测试。
- [x] 使用 Java 21 和 Gradle 9.2.1 执行 Gradle 测试套件。
- [x] 实现队伍成员服务和大厅、运行中、已结束的游戏会话生命周期。
- [x] 实现连线胜利自动结束和管理员主动结束游戏。
- [x] 为卡片、认领、队伍、生命周期、种子和胜者实现不可变快照。
- [x] 为运行中和已结束会话实现带校验的内存重启恢复。
- [x] 实现 5×5 物品目标会话的 NeoForge 世界存档适配器。
- [x] 实现 `/neobingo start`、`join`、`card` 和 `end` 命令。
- [x] 实现标准模式和一种独立设计、兼容原版客户端的宾果卡展示方式。
- [x] 通过服务器权威的物品栏检查手动认领物品目标。
- [x] 为格子判定、胜利条件、世界存档恢复和重连身份添加 GameTest。

## 阶段 2——游戏模式和配置

- [x] 为默认宾果卡目标池实现带版本且严格校验的数据包定义。
- [x] 实现可通过服务端命令选择的标准及锁定模式。
- [x] 实现大厅离队、种子查看和管理员重新生成卡片操作。
- [x] 为在线队员实现定期执行的服务器权威物品栏判定。
- [x] 提供原创的英文和简体中文命令反馈资源。
- [x] 将标准与锁定认领行为提取为独立规则策略。
- [x] 实现独立的隐藏可见性策略及可选隐藏模式。
- [x] 将物品栏目标完成判定提取为独立策略。
- [x] 实现带稳定并列处理的独立得分排名策略。
- [x] 将连线胜利与禁止自动胜利提取为独立策略。
- [x] 在领域层组合排位模式规则，并禁止其因连线提前结束。
- [x] 实现可快照与 NBT 持久化的排位倒计时及唯一领先者判定。
- [x] 实现带持久化时间限制、服务端推进和命令入口的完整排位模式。
- [x] 为排位倒计时结束及世界数据写回添加 GameTest。
- 将锁定、物品栏、隐藏和排位规则实现为彼此隔离的策略模块。
- [x] 增加带版本的 JSON Schema、严格验证和版本 0 到版本 1 的迁移测试。
- [x] 增加带严格往返验证和 Gradle 入口的宾果卡定义生成器。

## 阶段 3——客户端体验

- [x] 使用原创文本面板实现首个可选 NeoForge 宾果卡 HUD。
- [x] 实现可重新绑定按键打开的完整宾果卡界面。
- [x] 将完整卡片界面改为自适应网格，并限制 HUD 在窄屏下的宽度。
- [x] 增加单元格完整文本提示和可重新绑定的 HUD 开关。
- [x] 支持点击关注卡片格子，并在 HUD 中持续显示所关注的目标。
- 增加原创图形资源并完善界面交互。
- [x] 注册带版本号且可缺省的客户端增强协议通道。
- [x] 登录时仅向已协商通道的增强客户端发送协议版本。
- [x] 在保留聊天卡片的同时，向增强客户端同步结构化卡片快照。
- [x] 在开局、重新生成和认领后实时刷新增强客户端 HUD。
- [x] 重连时恢复 HUD，并在客户端断开连接时清除缓存。
- [x] 使用明确的可选协议协商机制，使原版客户端继续受到支持。

## 阶段 4——集成和加固

- 通过编译期 API 添加可选的配方查看器和语音聊天集成。
- 完善权限、性能测试、兼容性矩阵和发布自动化。

## 架构边界

- `domain`：与模组加载器无关的规则和不可变状态。
- `domain.rule`：由游戏模式选择的独立认领策略。
- `application`：命令和应用用例。
- `neoforge`：事件、持久化适配器、网络和生命周期。
- `client`：只包含可选界面和渲染。

领域层测试不得启动 Minecraft。NeoForge GameTest 用于验证适配器行为。
